# Public app URL used for canonical links, sitemap generation, and metadata.
NEXT_PUBLIC_APP_URL=https://www.investair.online

# Public backend API base URL used by the browser.
NEXT_PUBLIC_API_URL=https://investmentback.onrender.com

# Optional server-side override for backend API calls.
# Leave this aligned with NEXT_PUBLIC_API_URL unless the server should use a different internal URL.
BACKEND_URL=https://investmentback.onrender.com